# Windows-compatible FastAPI optimizations
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Existing dependencies (keep your current versions)
google-generativeai
pydantic
python-multipart
python-jose[cryptography]
passlib[bcrypt]
PyPDF2

# Windows-compatible performance optimizations
aiofiles>=23.2.1  # Async file operations
aiohttp>=3.9.0  # Async HTTP client for better API calls

# Optional: JSON optimization (works on Windows)
# orjson>=3.9.10  # Uncomment if you want faster JSON serialization
