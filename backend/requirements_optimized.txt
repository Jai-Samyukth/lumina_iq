# Core FastAPI and async optimizations
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
uvloop>=0.19.0  # Better async event loop
httptools>=0.6.1  # Faster HTTP parsing

# Existing dependencies (keep your current versions)
google-generativeai
pydantic
python-multipart
python-jose[cryptography]
passlib[bcrypt]
PyPDF2

# Additional performance optimizations
aiofiles>=23.2.1  # Async file operations
aiohttp>=3.9.0  # Async HTTP client for better API calls
orjson>=3.9.10  # Faster JSON serialization
